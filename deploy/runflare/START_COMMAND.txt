# فقط این یک خط را در:
# تنظیمات → «دستور پیش از شروع»
# بگذار (اگر قبلاً gunicorn گذاشتی پاکش کن و فقط این را بگذار):

bash /app/deploy/runflare/prestart.sh

# ---------------------------------------------------------------
# اگر جایی «دستور اصلی / Start / Command» جدا دیدی (نه پیش از شروع):
# باید ASGI باشد تا چت پشتیبانی و /api/v1 کار کند:
gunicorn koohestan.asgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers 2 --timeout 120

# در «تغییر اپلیکیشن» اگر فیلد نام پروژه / ماژول بود:
koohestan.asgi
